# HealthOS iOS

Create the Xcode SwiftUI app in this directory:

Xcode -> New Project -> iOS App (SwiftUI) -> Name: HealthOS
Save it to: ios/ (so ios/HealthOS.xcodeproj exists)

Then enable:
- Signing & Capabilities: HealthKit
- Info.plist: NSHealthShareUsageDescription
